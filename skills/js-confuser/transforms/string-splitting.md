# StringSplitting

Source: `transforms/string/stringSplitting.ts`

`"hello"` → `"he" + "ll" + "" + "o"` (binary `+` chain, random chunk sizes).

## Reversal

Constant-fold binary `+` on string literals.
