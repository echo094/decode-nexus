# Finalizer

Source: `transforms/finalizer.ts`

Applies [StringEncoding](string-encoding.md) (cosmetic) and optional hex-number literals
(`0x1a` instead of `26`) — both are free for Babel's parser, nothing to build.
